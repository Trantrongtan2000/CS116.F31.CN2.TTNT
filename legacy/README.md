# Legacy (superseded)

These are the original files from before the clean rebuild. They are kept for
reference only and are **not** part of the working system. Known issues that
motivated the rebuild: the training script never completed, reported metrics were
not backed by any run, and the demo did not launch. See `../README.md` and
`../AUDIT.md` for details, and `../mrc/` + the top-level `run_*.py` scripts for the
current pipeline.
