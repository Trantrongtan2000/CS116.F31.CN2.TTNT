# CS116 T11 - Vietnamese Extractive MRC (CPU-only, inference-first)
# Python 3.12 recommended (torch has no 3.14 wheels yet).

# Core ML / NLP
torch>=2.2
transformers>=4.44,<5   # 5.x removed the extractive "question-answering" pipeline
tokenizers>=0.15
tiktoken>=0.7          # required by transformers 5.x to load XLM-R tokenizers
sentencepiece>=0.2    # XLM-R / multilingual tokenizers
protobuf>=4.0

# Baseline + metrics + analysis
scikit-learn>=1.4
numpy>=1.26

# Visualization
matplotlib>=3.8
seaborn>=0.13

# Web demo
streamlit>=1.32

# Utilities
tqdm>=4.66
huggingface-hub>=0.21

# Dev / testing
pytest>=8.0
